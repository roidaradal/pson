# pson 
A tool for prettifying, aligning, and compressing JSON files

## Installation 
`go install github.com/roidaradal/pson` 

## Usage 
`pson <pretty|align|compress> <file.json> (--overwrite)`

## TODO
* `pretty` command 
* `align` command 