# pson 
A tool for indenting, aligning, and compressing JSON files

## Installation 
`go install github.com/roidaradal/pson@latest` 

## Usage 
`pson <indent|align|compress> <file.json> (--overwrite) (--indent=2) (--flatlist)`
